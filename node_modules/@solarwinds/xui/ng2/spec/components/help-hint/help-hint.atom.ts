import {promise} from "@solarwinds/uzi/protractor";
import {Atom} from "@solarwinds/uzi";

export class HelpHintAtom extends Atom {
    public static CSS_CLASS = "xui-help-hint";

    public getHelpText() {
        return super.getElement().getText();
    }
}
