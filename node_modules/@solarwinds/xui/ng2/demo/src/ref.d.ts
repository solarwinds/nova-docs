/**
 * We need to have an ambient declaration of the module that we're importing from the demo.
 * It's declared as an external in webpack and is retrieved through global variable called "Xui" - see gulpfile.js
 */
declare module "xui" {
    const _temp: any;
    export = _temp;
}
