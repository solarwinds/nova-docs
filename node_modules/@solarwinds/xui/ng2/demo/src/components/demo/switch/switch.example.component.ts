import {Component, Input} from "@angular/core";

@Component({
    selector: "xui-switch-example",
    template: require<string>("./switch.example.component.html")
})

export class SwitchExampleComponent {
    @Input() switchVal: boolean = true;
    isDisabled: boolean = true;
}
