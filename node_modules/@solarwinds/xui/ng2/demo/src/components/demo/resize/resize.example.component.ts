import { Component, ElementRef, Inject, ViewChild } from "@angular/core";

@Component({
    selector: "xui-resize-demo",
    template: require<string>("./resize.example.component.html")
})
export class ResizeExampleComponent {

    public checkResize = (): void => {
        console.log("resize FIRED!");
    };

    public checkNestedResize = (): void => {
        console.log("nested resize FIRED!");
    };

    public checkSpanResize = (): void => {
        console.log("span resize FIRED!");
    };
}
