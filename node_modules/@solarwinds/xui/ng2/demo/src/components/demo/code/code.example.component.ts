import { Component } from "@angular/core";

@Component({
    template: require<string>("./code.example.component.html")
})
export class CodeExampleComponent {
    public code = `export class Code implements ng.IDirective {
            public template = require<string>(\"./code-directive.html\");
            public link = (scope:ng.IScope, element:ng.IAugmentedJQuery,
                   attrs:ng.IAttributes) => {
               // MAGIC! 
            };
        }`;
}
