import { ElementFinder, promise } from "@solarwinds/uzi/protractor";
import { Atom } from "@solarwinds/uzi";
/**
 * Lukas: WARNING FOR TRESPASSERS
 *
 * For anyone who's going to touch datepicker e2e tests - this atom doesn't have proper API for simply setting date
 * value to the field ... something like setValue(date:Date), or setValue(int year, int month, int day) would be
 * really nice to have, because the users don't want to mess with internal structure of the datepicker component.
 * Current API is more likely for internal testing only.
 */
export declare class DatePickerAtom extends Atom {
    static CSS_CLASS: string;
    static MONTHNAMES_SHORT: string[];
    static MONTHNAMES_LONG: string[];
    static ACCEPTED_DATES: string[];
    clickTitle(): promise.Promise<void>;
    getPopup(): ElementFinder;
    clickCalendarIcon(): promise.Promise<void>;
    isOpened(): promise.Promise<boolean>;
    clickInput(): promise.Promise<void>;
    clickToday(): promise.Promise<void>;
    formatDate(date: Date): string;
    getInput(): ElementFinder;
    getInputValue(): promise.Promise<string>;
    acceptText(value: string): promise.Promise<void>;
    acceptKeys(...keys: any[]): promise.Promise<void>;
    clearText(): promise.Promise<void>;
    getTitle(): promise.Promise<string>;
    getNextTitle(): string;
    goNext(): promise.Promise<void>;
    goBack(): promise.Promise<void>;
    selectDay(day: number, index?: number): promise.Promise<void>;
    selectMonth(month: string, index?: number): promise.Promise<void>;
    selectYear(year: string, index?: number): promise.Promise<void>;
    getYearElement(year: string, index?: number): ElementFinder;
    getMonthElement(month: string, index?: number): ElementFinder;
    getActiveDay(): promise.Promise<string>;
    isValid(): promise.Promise<boolean>;
    getElementTest(className: string): ElementFinder;
    private getClassValueByCss(title);
    private getClassValueByButtonText(title, index);
    private clickButton(buttonTitle, index?);
    private getElementValue(identifier, deep?);
    private getElementText(identifier, deep?);
    private clickElement(identifier);
    private getElementByCss(identifier, deep?);
    private selectButton(identifier, index?);
}
