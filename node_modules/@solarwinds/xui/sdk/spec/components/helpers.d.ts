import { WebElementPromise } from "@solarwinds/uzi/webdriver";
import { Eyes } from "eyes.protractor";
export default class Helpers {
    static prepareBrowser(pageName?: string): Promise<void>;
    static prepareEyes(): Eyes;
    static getElementByXpath(elementXpath: string): WebElementPromise;
    static getElementByCSS(elementCSS: string): WebElementPromise;
}
