"use strict";

const gulp = require("gulp");
const $ = require("@solarwinds/buildmeister/gulp-load-plugins")(gulp);

/**
 * Replace paths in ng-docs files
 */
gulp.task("clean:root-dist", function () {
    return $.del(["../dist", "../typings"], {force: true});
});
