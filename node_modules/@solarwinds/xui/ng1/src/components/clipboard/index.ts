import Module from "../../common/module";
import Clipboard from "./clipboard-directive";
import ClipboardController from "./clipboard-controller";

export default (module:Module)=> {
    module.component("xuiClipboard", Clipboard);
    module.controller("xuiClipboardController", ClipboardController);
};
