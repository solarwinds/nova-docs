(function() {
    "use strict";

    Xui.module
        .controller("DemoClipboardController", DemoClipboardController, ["xuiToastService"]);

    function DemoClipboardController(toast) {
        var vm = this;

        vm.textToCopy = "text to copy";

        vm.onClipboardSuccess = function() {
            toast.success("Text was copied to clipboard.");
        };

        vm.onClipboardError = function(ex) {
            toast.error(ex.message);
        };
    }
})();
