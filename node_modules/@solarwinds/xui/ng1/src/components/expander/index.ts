import Module from "../../common/module";

import { ExpanderController }           from "./expander-controller";
import { Expander } from "./expander-directive";

export default (module: Module)=> {
    module.component("xuiExpander", Expander);
    module.controller("xuiExpanderController", ExpanderController);
};
