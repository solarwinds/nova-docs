import Module from "../../common/module";
import Busy from "./busy-directive";
import BusyController from "./busy-controller";

export default (module:Module)=> {
    module.component("xuiBusy", Busy);
    module.controller("xuiBusyController", BusyController);
};
