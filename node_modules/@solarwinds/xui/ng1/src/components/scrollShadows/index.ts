import Module from "../../common/module";
import { ScrollShadows } from "./scroll-shadows-directive";

export default (module: Module) => {
    module.component("xuiScrollShadows", ScrollShadows);
};
