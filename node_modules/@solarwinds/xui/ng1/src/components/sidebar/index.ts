import Module from "../../common/module";
import Sidebar from "./sidebar-directive";
import SidebarContainer from "./sidebar-container-directive";
import SidebarController from "./sidebar-controller";

export default (module: Module) => {
    module.component("xuiSidebar", Sidebar);
    module.component("xuiSidebarContainer", SidebarContainer);
    module.controller("xuiSidebarController", SidebarController);
};
