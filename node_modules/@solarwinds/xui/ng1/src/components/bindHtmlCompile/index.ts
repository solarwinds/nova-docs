import Module from "../../common/module";
import BindHtmlCompile from "./bindHtmlCompile-directive";

export default (module: Module) => {
    module.component("xuiBindHtmlCompile", BindHtmlCompile);
};
