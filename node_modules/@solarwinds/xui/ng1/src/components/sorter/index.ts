import Module from "../../common/module";

import Sorter from "./sorter-directive";
import SorterController from "./sorter-controller";

export default (module:Module)=> {
    module.component("xuiSorter", Sorter);
    module.controller("xuiSorterController", SorterController);
};
