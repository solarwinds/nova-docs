import Module from "../../common/module";
import { ActionDrawer } from "./actionDrawer-directive";
import { ActionDrawerController } from "./actionDrawer-controller";

export default (module: Module) => {
    module.component("xuiActionDrawer", ActionDrawer);
    module.controller("xuiActionDrawerController", ActionDrawerController);
};
