import Module from "../../common/module";

import Slider from "./slider-directive";
import SliderController from "./slider-controller";

export default (module: Module)=> {
    module.component("xuiSlider", Slider);
    module.controller("xuiSliderController", SliderController);
};
