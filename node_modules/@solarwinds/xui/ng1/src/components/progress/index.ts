import Module from "../../common/module";
import Progress from "./progress-directive";
import xuiStatic from "../../xui-static";

export default (module:Module)=> {
    module.component("xuiProgress", Progress);

    const translations = require("./progress-translations.json") as {[key:string]:string};
    module.app("components").config(xuiStatic.registerTranslations(translations));
};
