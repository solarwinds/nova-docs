import Module from "../../common/module";
import { Breadcrumb } from "./breadcrumb-directive";
import { BreadcrumbController } from "./breadcrumb-controller";

export default (module:Module)=> {
    module.component("xuiBreadcrumb", Breadcrumb);
    module.controller("xuiBreadcrumbController", BreadcrumbController);
};
