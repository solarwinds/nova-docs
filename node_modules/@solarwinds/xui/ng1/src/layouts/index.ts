import Module from "../common/module";
import StripLayout from "./strip/strip-layout-directive";
import StripLayoutItem from "./strip/strip-layout-item-directive";
import StripLayoutSplitter from "./strip/strip-layout-splitter-directive";

export default (module:Module) => {
    module.component("xuiStripLayout", StripLayout);
    module.component("xuiStripLayoutItem", StripLayoutItem);
    module.component("xuiStripLayoutSplitter", StripLayoutSplitter);
};
