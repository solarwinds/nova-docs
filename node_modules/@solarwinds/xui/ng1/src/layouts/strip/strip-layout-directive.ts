/// <reference path="../../ref.d.ts" />

class StripLayout implements ng.IDirective {

    // this directive is just for internal usage

    public restrict = "E";
    public template = "<div class='xui-strip-layout xui-flex-container' ng-transclude></div>";
    public replace = true;
    public transclude = true;
}

export default StripLayout;
