function dateFilter($filter:ng.IFilterService) {
    return function (date:string, format:string, timezone:any) {
        if (!date) {
            return null;
        }
        return $filter("date")(new Date(parseInt(date.substr(6), 10)), format, timezone);
    };
}
dateFilter.$inject = ["$filter"];

export default dateFilter;
