/// <reference path="../ref.d.ts" />

export default function Inject(dependency:string) {
    return (prototype:Function, method:string | symbol, argumentPosition:number) => {
        prototype.$inject = prototype.$inject || [];
        prototype.$inject[argumentPosition] = dependency;
    };
}
