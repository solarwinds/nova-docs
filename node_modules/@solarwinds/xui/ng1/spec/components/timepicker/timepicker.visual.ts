import {browser, promise} from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import {Eyes} from "eyes.protractor";
import {TimepickerAtom} from "../index.atom";

describe("Visual tests: Timepicker", function() {
    let eyes: Eyes;
    let timepicker: TimepickerAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("timepicker");
    });

    it("Default look", function() {
        eyes.open(browser, "XUI","Timepicker");
        eyes.checkWindow("Default");
        eyes.close();
    });
});
