import {browser, promise} from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import {Eyes} from "eyes.protractor";
import {TextboxAtom} from "../index.atom";

describe("Visual tests: Textbox", function() {
    let eyes: Eyes;
    let textbox: TextboxAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("textbox");
    });

    it("Default look", function() {
        eyes.open(browser, "XUI","Textbox");
        eyes.checkWindow("Default");
        eyes.close();
    });
});
