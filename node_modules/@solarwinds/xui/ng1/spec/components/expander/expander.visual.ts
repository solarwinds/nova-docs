import {browser, promise} from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import {Eyes} from "eyes.protractor";
import {ExpanderAtom} from "../index.atom";

describe("Visual tests: Expander", function() {
    let eyes: Eyes;
    let expander: ExpanderAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("expander");
    });

    it("Default look", function() {
        eyes.open(browser, "XUI","Expander");
        eyes.checkWindow("Default");
        eyes.close();
    });
});
