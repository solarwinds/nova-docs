import {browser, promise} from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import {Eyes} from "eyes.protractor";
import {MessageAtom} from "../index.atom";

describe("Visual tests: Message", function() {
    let eyes: Eyes;
    let message: MessageAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("message");
    });

    it("Default look", function() {
        eyes.open(browser, "XUI","Message");
        eyes.checkWindow("Default");
        eyes.close();
    });
});
