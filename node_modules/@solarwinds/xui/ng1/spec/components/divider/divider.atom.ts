import {promise} from "@solarwinds/uzi/protractor";
import {Atom} from "@solarwinds/uzi";

export class DividerAtom extends Atom {
    public static CSS_CLASS = "xui-divider";

    public getBorderTopValue(): promise.Promise<string> {
        return super.getElement().getCssValue("border-top");
    }
}
