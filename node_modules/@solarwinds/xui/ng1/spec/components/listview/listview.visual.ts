import {browser, promise} from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import {Eyes} from "eyes.protractor";
import {ListViewAtom} from "../index.atom";

describe("Visual tests: Listview", function() {
    let eyes: Eyes;
    let listview: ListViewAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("listview");
    });

    it("Default look", function() {
        eyes.open(browser, "XUI","ListView");
        eyes.checkWindow("Default");
        eyes.close();
    });
});
