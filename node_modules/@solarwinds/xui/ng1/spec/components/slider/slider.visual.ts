import {browser, promise} from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import {Eyes} from "eyes.protractor";
import {SliderAtom} from "../index.atom";

describe("Visual tests: Slider", function() {
    let eyes: Eyes;
    let sorter: SliderAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("slider");
    });
 
    it("Default look, label alignment included", function() {
        eyes.open(browser, "XUI","Slider");
        eyes.checkWindow("Default");
        eyes.close();
    });
});
