import { browser } from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import { Eyes } from "eyes.protractor";
import { ContentAtom } from "../index.atom";

describe("Visual tests: Code Item", function() {
    let eyes: Eyes;
    let content: ContentAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("codeItem");
    });

    it("Default look", function() {
        eyes.open(browser, "XUI", "CodeItem");
        eyes.checkWindow("Default");
        eyes.close();
    });
});
