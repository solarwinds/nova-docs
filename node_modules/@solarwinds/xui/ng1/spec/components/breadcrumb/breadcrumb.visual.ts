import { browser, by, element, promise } from "@solarwinds/uzi/protractor";
import { Eyes } from "eyes.protractor";
import { BreadcrumbAtom } from "../index.atom";
import Helpers from "../helpers";

describe("Visual tests", function () {
    let eyes: Eyes;
    let breadcrumb: BreadcrumbAtom;

    beforeAll(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("breadcrumb");
    });

    it("for breadcrumb", function () {
        eyes.open(browser, "XUI", "Breadcrumb");
        eyes.checkWindow("Default");
        eyes.close();     
    });
});
