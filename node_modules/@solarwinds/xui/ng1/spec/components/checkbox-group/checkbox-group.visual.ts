import {browser, promise} from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import {Eyes} from "eyes.protractor";

describe("Visual tests: Checkbox-group", function() {
    let eyes: Eyes;
    //let checkboxGroup: CheckboxGroupAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("checkbox-group");
    });

    it("Default look", function() {
        eyes.open(browser, "XUI", "Checkbox-group");
        eyes.checkWindow("Default");
        eyes.close();
    });
});
