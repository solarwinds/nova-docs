import {browser, by, promise} from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import {Eyes} from "eyes.protractor";
import {ResizableAtom} from "../index.atom";

describe("Visual tests: Resizable", function() {
    let eyes: Eyes;
    let resizable: ResizableAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("resizable");
    });

    it("Default look", function() {
        /* empty */
    });
});
