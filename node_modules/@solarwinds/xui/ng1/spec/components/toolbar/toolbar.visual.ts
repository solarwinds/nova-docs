import {browser, promise} from "@solarwinds/uzi/protractor";
import Helpers from "../helpers";
import {Eyes} from "eyes.protractor";
import {ToolbarAtom} from "../index.atom";

describe("Visual tests: Toolbar", function() {
    let eyes: Eyes;
    let toolbar: ToolbarAtom;
    beforeEach(() => {
        eyes = Helpers.prepareEyes();
        Helpers.prepareBrowser("toolbar");
    });

    it("Default look", function() {
        eyes.open(browser, "XUI","Toolbar");
        eyes.checkWindow("Default");
        eyes.close();
    });
});
